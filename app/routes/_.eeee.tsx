import { redirect } from "@remix-run/node";

export default function EEEE() {
    return (
        <div>
            <button onClick={() => console.log("SHIP HAI")}>
                EI AU
            </button>
        </div>
    );
}
